# Generated by Cloud66 Starter
---
services:

  web:
    git: http://github.com/babakgh/websocket-rails-demo.git
    git_branch: master
    command: bundle exec rails s _env:RAILS_ENV
    build_root: .
    ports:
      - container: 3000
        http: 80
        https: 443
    env_vars:
      RAILS_ENV: production
      RACK_ENV: production

databases:
  - postgresql
